ext.cdvMinSdkVersion=29
ext.cdvReleaseSigningPropertiesFile="../../release-signing.properties"

android {
    defaultConfig {
        minSdkVersion 29
        targetSdkVersion 29

        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a"
        }
    }
}

// abiFilters "armeabi-v7a", "arm64-v8a", "x86", "x86_64"
