ext.cdvMinSdkVersion=24

android {
    defaultConfig {
        minSdkVersion 24
        targetSdkVersion 26

        ndk {
            abiFilters "armeabi-v7a", "arm64-v8a"
        }
    }
}

// abiFilters "armeabi-v7a", "arm64-v8a", "x86", "x86_64"
